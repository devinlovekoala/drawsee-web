export const NODE_WIDTH = 900; // 从800增加到900，让节点内容更清晰可见
export const NODE_DEFAULT_HEIGHT = 240; // 从200增加到240，提供更多垂直空间
export const NODE_MIN_HEIGHT = 120; // 从100增加到120，确保最小节点也足够清晰
export const ROOT_NODE_SIZE = 220; // 从200增加到220，根节点稍微大一点
export const ROOT_NODE_SVG_SIZE = '2.8em'; // 从2.6em增加到2.8em，图标也相应放大
export const TEMP_QUERY_NODE_HEIGHT = 240; // 从200增加到240，与默认高度保持一致
export const TEMP_QUERY_NODE_ID_PREFIX = 'temp-query-';
